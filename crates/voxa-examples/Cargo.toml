[package]
name = "voxa-examples"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
voxa-core = { path = "../voxa-core" }
voxa-types = { path = "../voxa-types" }
